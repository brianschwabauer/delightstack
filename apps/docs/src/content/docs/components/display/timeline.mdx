---
title: Timeline
description: A chronological event display for vertical or horizontal timelines with scroll-reveal animations, status markers, and on-demand loading.
---

import ComponentPreview from '../../../../components/ComponentPreview.astro';
import TimelineDemo from '../../../../components/demos/TimelineDemo.svelte';
import TimelineHorizontalDemo from '../../../../components/demos/TimelineHorizontalDemo.svelte';
import TimelineColorDemo from '../../../../components/demos/TimelineColorDemo.svelte';
import TimelineOrderDemo from '../../../../components/demos/TimelineOrderDemo.svelte';
import TimelineAlternateDemo from '../../../../components/demos/TimelineAlternateDemo.svelte';
import TimelineLoadMoreDemo from '../../../../components/demos/TimelineLoadMoreDemo.svelte';
import TimelineSkeletonDemo from '../../../../components/demos/TimelineSkeletonDemo.svelte';
import TimelineInteractiveDemo from '../../../../components/demos/TimelineInteractiveDemo.svelte';

## Import

```svelte
import { Timeline, TimelineItem } from '@delightstack/components';
```

The `TimelineItem` is re-exported from the `Timeline` module. Both `Timeline` and `TimelineItem` are backed by the same Svelte component -- when used inside a Timeline context, it renders as an item; at the top level, it renders as the container.

## Basic Usage

<ComponentPreview code={`<Timeline>
${'  '}<TimelineItem title="Step 1" date="Jan 10" status="complete">
${'  '}${'  '}First step completed.
${'  '}</TimelineItem>
${'  '}<TimelineItem title="Step 2" date="Jan 11" status="active">
${'  '}${'  '}Currently in progress.
${'  '}</TimelineItem>
${'  '}<TimelineItem title="Step 3" status="pending" />
</Timeline>`}>
  <TimelineDemo client:load />
</ComponentPreview>

## Examples

### Order Tracking

<ComponentPreview code={`<script>
${'  '}import { Timeline, TimelineItem } from '@delightstack/components';
</script>

<Timeline>
${'  '}<TimelineItem status="complete" date="Jan 10" title="Order Confirmed">
${'  '}${'  '}Your order has been confirmed.
${'  '}</TimelineItem>
${'  '}<TimelineItem status="complete" date="Jan 11" title="Shipped">
${'  '}${'  '}Package picked up by carrier.
${'  '}</TimelineItem>
${'  '}<TimelineItem status="active" date="Jan 12" title="Out for Delivery">
${'  '}${'  '}Expected delivery today.
${'  '}</TimelineItem>
${'  '}<TimelineItem status="pending" title="Delivered" />
</Timeline>`}>
  <TimelineOrderDemo client:load />
</ComponentPreview>

### Horizontal Process Steps

<ComponentPreview code={`<Timeline horizontal>
${'  '}<TimelineItem status="complete" title="Order Placed" />
${'  '}<TimelineItem status="complete" title="Processing" />
${'  '}<TimelineItem status="active" title="Shipping" />
${'  '}<TimelineItem status="pending" title="Delivered" />
</Timeline>`}>
  <TimelineHorizontalDemo client:load />
</ComponentPreview>

### Alternating Sides

Events alternate between left and right in vertical mode.

<ComponentPreview code={`<Timeline alternate>
${'  '}<TimelineItem date="Jan 1" title="Event A">Description A</TimelineItem>
${'  '}<TimelineItem date="Feb 1" title="Event B">Description B</TimelineItem>
${'  '}<TimelineItem date="Mar 1" title="Event C">Description C</TimelineItem>
</Timeline>`}>
  <TimelineAlternateDemo client:load />
</ComponentPreview>

### On-Demand Loading

Load more events as the user scrolls near the end of the timeline.

<ComponentPreview code={`<Timeline onloadmore={loadMoreEvents} pending>
${'  '}{#each events as event}
${'  '}${'  '}<TimelineItem
${'  '}${'  '}${'  '}date={event.date}
${'  '}${'  '}${'  '}title={event.title}
${'  '}${'  '}${'  '}status={event.status}
${'  '}${'  '}>
${'  '}${'  '}${'  '}{event.description}
${'  '}${'  '}</TimelineItem>
${'  '}{/each}
</Timeline>`}>
  <TimelineLoadMoreDemo client:load />
</ComponentPreview>

### Interactive Items

Give an item an `onclick` and/or `href` to make it clickable (mirroring `<Button>`). Interactive items get a pointer cursor, a hover tint, a ripple, and a press effect; items without either stay static.

<ComponentPreview code={`<Timeline>
${'  '}<TimelineItem
${'  '}${'  '}status="complete"
${'  '}${'  '}date="Jan 10"
${'  '}${'  '}title="Order Confirmed"
${'  '}${'  '}onclick={() => (selected = 'Order Confirmed')}>
${'  '}${'  '}Tap to view the confirmation.
${'  '}</TimelineItem>
${'  '}<TimelineItem status="active" date="Jan 12" title="Out for Delivery" href="/orders/123" />
</Timeline>`}>
  <TimelineInteractiveDemo client:load />
</ComponentPreview>

### Custom Marker Color

<ComponentPreview code={`<Timeline>
${'  '}<TimelineItem title="Custom Color" color="var(--color-error)">
${'  '}${'  '}A red-themed event marker.
${'  '}</TimelineItem>
</Timeline>`}>
  <TimelineColorDemo client:load />
</ComponentPreview>

### Skeleton Loading

<ComponentPreview code={`<Timeline skeleton={loading} skeleton_count={3}>…</Timeline>`}>
  <TimelineSkeletonDemo client:load />
</ComponentPreview>

## Timeline Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `horizontal` | `boolean` | `false` | Horizontal layout instead of vertical |
| `alternate` | `boolean` | `false` | Alternate items left/right (vertical) or top/bottom (horizontal) |
| `pending` | `boolean` | `false` | Show a pending/loading indicator at the end |
| `dense` | `boolean` | `false` | Compact event spacing |
| `comfortable` | `boolean` | `false` | Relaxed event spacing |
| `animate` | `boolean` | `true` | Play the entrance reveal + active pulse. Set `false` for a fully static timeline. (The reveal is also skipped automatically when content replaces a skeleton.) |
| `skeleton` | `boolean` | `false` | Show loading skeleton |
| `skeleton_count` | `number` | `3` | Number of skeleton items |
| `id` | `string` | auto | Element ID |
| `class` | `string` | `''` | Additional CSS classes |
| `children` | `Snippet` | - | TimelineItem children |

## Timeline Events

| Event | Detail | Description |
|-------|--------|-------------|
| `onloadmore` | `() => void \| Promise<void>` | Called when scrolling near the end for on-demand loading |

## TimelineItem Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `date` | `Date \| string` | - | Event timestamp |
| `title` | `string` | `''` | Event title |
| `icon` | `Component` | - | Marker icon component |
| `color` | `string` | - | Marker color override (CSS color value) |
| `status` | `'complete' \| 'active' \| 'pending'` | - | Event status for marker styling |
| `href` | `string` | - | Makes the item a link (renders an `<a>`). Enables hover/ripple/press effects |
| `onclick` | `(event) => void` | - | Click handler. Makes the item interactive (hover/ripple/press effects) |
| `target` | `'_self' \| '_blank' \| '_parent' \| '_top'` | - | Link target (used with `href`) |
| `children` | `Snippet` | - | Event body content |

## Types

The Timeline exports a `TimelineContext` interface for internal context:

```typescript
interface TimelineContext {
  horizontal: boolean;
  alternate: boolean;
  dense: boolean;
  comfortable: boolean;
  animate: boolean;
  reveal: boolean;
  register: () => number;
}
```

## Accessibility

- Timeline renders as an ordered list (`<ol>`) with `role="list"` for semantic structure
- Dates use `<time>` elements with `datetime` attributes
- Status is communicated visually through marker styling (a checked node for complete, a glowing/pulsing node for active, a hollow ring for pending)
- Scroll-reveal animations respect `prefers-reduced-motion` -- items appear without animation when reduced motion is preferred
- The load-more sentinel and skeleton items use `aria-hidden="true"`
- Horizontal mode supports native touch/swipe scrolling with CSS snap points
