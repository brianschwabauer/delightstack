---
title: Tree
description: A hierarchical data display component for folder structures and nested data with expand/collapse, checkboxes, drag-and-drop, lazy loading, and search filtering.
---

import ComponentPreview from '../../../../components/ComponentPreview.astro';
import TreeDemo from '../../../../components/demos/TreeDemo.svelte';
import TreeSelectableIconsDemo from '../../../../components/demos/TreeSelectableIconsDemo.svelte';
import TreeCheckboxDemo from '../../../../components/demos/TreeCheckboxDemo.svelte';
import TreeFlatDataDemo from '../../../../components/demos/TreeFlatDataDemo.svelte';
import TreeSearchDemo from '../../../../components/demos/TreeSearchDemo.svelte';
import TreeDragDropDemo from '../../../../components/demos/TreeDragDropDemo.svelte';
import TreeLazyLoadDemo from '../../../../components/demos/TreeLazyLoadDemo.svelte';
import TreeLinesDemo from '../../../../components/demos/TreeLinesDemo.svelte';
import TreeCustomNodeDemo from '../../../../components/demos/TreeCustomNodeDemo.svelte';
import TreeDensityDemo from '../../../../components/demos/TreeDensityDemo.svelte';
import TreeSkeletonDemo from '../../../../components/demos/TreeSkeletonDemo.svelte';

## Import

```svelte
import { Tree } from '@delightstack/components';
import type { TreeNode, FlatTreeNode } from '@delightstack/components';
```

## Basic Usage

<ComponentPreview code={`<script>
${'  '}import { Tree } from '@delightstack/components';

${'  '}const data = [
${'  '}${'  '}{
${'  '}${'  '}${'  '}id: 'src',
${'  '}${'  '}${'  '}label: 'src',
${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}{
${'  '}${'  '}${'  '}${'  '}${'  '}id: 'components',
${'  '}${'  '}${'  '}${'  '}${'  '}label: 'components',
${'  '}${'  '}${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: 'button', label: 'Button.svelte' },
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: 'modal', label: 'Modal.svelte' },
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: 'table', label: 'Table.svelte' },
${'  '}${'  '}${'  '}${'  '}${'  '}],
${'  '}${'  '}${'  '}${'  '}},
${'  '}${'  '}${'  '}${'  '}{
${'  '}${'  '}${'  '}${'  '}${'  '}id: 'utils',
${'  '}${'  '}${'  '}${'  '}${'  '}label: 'utils',
${'  '}${'  '}${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: 'format', label: 'format.ts' },
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: 'validate', label: 'validate.ts' },
${'  '}${'  '}${'  '}${'  '}${'  '}],
${'  '}${'  '}${'  '}${'  '}},
${'  '}${'  '}${'  '}${'  '}{ id: 'app', label: 'app.ts' },
${'  '}${'  '}${'  '}${'  '}{ id: 'index', label: 'index.ts' },
${'  '}${'  '}${'  '}],
${'  '}${'  '}},
${'  '}${'  '}{ id: 'package', label: 'package.json' },
${'  '}${'  '}{ id: 'tsconfig', label: 'tsconfig.json' },
${'  '}${'  '}{ id: 'readme', label: 'README.md' },
${'  '}];

${'  '}let expanded = $state(['src', 'components']);
</script>

<Tree {data} bind:expanded show_lines />`}>
  <TreeDemo client:load />
</ComponentPreview>

## Examples

### Selectable with Icons

<ComponentPreview code={`<script>
${'  '}import { Tree } from '@delightstack/components';

${'  '}const fileTree = [
${'  '}${'  '}{
${'  '}${'  '}${'  '}id: '1',
${'  '}${'  '}${'  '}label: 'src',
${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}{
${'  '}${'  '}${'  '}${'  '}${'  '}id: '1-1',
${'  '}${'  '}${'  '}${'  '}${'  '}label: 'components',
${'  '}${'  '}${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: '1-1-1', label: 'Button.svelte' },
${'  '}${'  '}${'  '}${'  '}${'  '}${'  '}{ id: '1-1-2', label: 'Modal.svelte' },
${'  '}${'  '}${'  '}${'  '}${'  '}],
${'  '}${'  '}${'  '}${'  '}},
${'  '}${'  '}${'  '}${'  '}{ id: '1-2', label: 'app.ts' },
${'  '}${'  '}${'  '}],
${'  '}${'  '}},
${'  '}${'  '}{ id: '2', label: 'package.json' },
${'  '}];

${'  '}let selected = $state([]);
${'  '}let expanded = $state(['1']);
</script>

<Tree
${'  '}data={fileTree}
${'  '}selectable
${'  '}bind:selected
${'  '}bind:expanded
/>`}>
  <TreeSelectableIconsDemo client:load />
</ComponentPreview>

### Checkbox Selection

Parent checkboxes reflect children state with an indeterminate state when partially selected. Checking a parent checks all children.

<ComponentPreview code={`<script>
${'  '}import { Tree } from '@delightstack/components';

${'  '}const categories = [
${'  '}${'  '}{
${'  '}${'  '}${'  '}id: 'fruits',
${'  '}${'  '}${'  '}label: 'Fruits',
${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}{ id: 'apple', label: 'Apple' },
${'  '}${'  '}${'  '}${'  '}{ id: 'banana', label: 'Banana' },
${'  '}${'  '}${'  '}${'  '}{ id: 'cherry', label: 'Cherry' },
${'  '}${'  '}${'  '}],
${'  '}${'  '}},
${'  '}${'  '}{
${'  '}${'  '}${'  '}id: 'vegetables',
${'  '}${'  '}${'  '}label: 'Vegetables',
${'  '}${'  '}${'  '}children: [
${'  '}${'  '}${'  '}${'  '}{ id: 'carrot', label: 'Carrot' },
${'  '}${'  '}${'  '}${'  '}{ id: 'broccoli', label: 'Broccoli' },
${'  '}${'  '}${'  '}${'  '}{ id: 'spinach', label: 'Spinach' },
${'  '}${'  '}${'  '}],
${'  '}${'  '}},
${'  '}];

${'  '}let selected = $state([]);
${'  '}let expanded = $state(['fruits', 'vegetables']);
</script>

<Tree data={categories} checkboxes multi_select bind:selected bind:expanded />`}>
  <TreeCheckboxDemo client:load />
</ComponentPreview>

### Flat Data with Parent IDs

The component automatically detects flat data (arrays with `parentId` properties) and builds the tree structure internally.

<ComponentPreview code={`<Tree
${'  '}data={[
${'  '}${'  '}{ id: '1', parentId: null, label: 'Root' },
${'  '}${'  '}{ id: '2', parentId: '1', label: 'Child 1' },
${'  '}${'  '}{ id: '3', parentId: '1', label: 'Child 2' },
${'  '}${'  '}{ id: '4', parentId: '2', label: 'Grandchild 1' },
${'  '}${'  '}{ id: '5', parentId: '2', label: 'Grandchild 2' },
${'  '}${'  '}{ id: '6', parentId: '3', label: 'Grandchild 3' },
${'  '}]}
${'  '}selectable
/>`}>
  <TreeFlatDataDemo client:load />
</ComponentPreview>

### Search/Filter

Nodes matching the filter term are highlighted. Non-matching branches without matching descendants are hidden, and parent nodes auto-expand to reveal matches.

<ComponentPreview code={`<script>
${'  '}import { Tree } from '@delightstack/components';

${'  '}let searchTerm = $state('');
</script>

<input bind:value={searchTerm} placeholder="Search..." />
<Tree data={fileTree} filter={searchTerm} />`}>
  <TreeSearchDemo client:load />
</ComponentPreview>

### Drag-and-Drop

Enable drag-and-drop reordering between nodes.

<ComponentPreview code={`<Tree
${'  '}data={fileTree}
${'  '}draggable
${'  '}ondrop={({ node, target, position }) => {
${'  '}${'  '}updateTree(node, target, position);
${'  '}}}
/>`}>
  <TreeDragDropDemo client:load />
</ComponentPreview>

### Lazy Loading

Load children on demand when a node is expanded. A loading spinner replaces the chevron during the fetch.

<ComponentPreview code={`<Tree
${'  '}data={rootNodes}
${'  '}load_children={async (node) => {
${'  '}${'  '}return await fetchChildren(node.id);
${'  '}}}
/>`}>
  <TreeLazyLoadDemo client:load />
</ComponentPreview>

### Connecting Lines

<ComponentPreview code={`<Tree data={fileTree} show_lines bind:expanded />`}>
  <TreeLinesDemo client:load />
</ComponentPreview>

### Custom Node Content

<ComponentPreview code={`{#snippet customNode({ node, level })}
${'  '}<span>{node.label}</span>
${'  '}{#if node.data?.badge}
${'  '}${'  '}<span class="badge">{node.data.badge}</span>
${'  '}{/if}
{/snippet}

<Tree data={fileTree} node_content={customNode} />`}>
  <TreeCustomNodeDemo client:load />
</ComponentPreview>

### Density

Use `dense` for compact node spacing or `comfortable` for relaxed spacing. The default sits between the two.

<ComponentPreview code={`<Tree data={fileTree} dense show_lines />

<Tree data={fileTree} comfortable show_lines />`}>
	<TreeDensityDemo client:load />
</ComponentPreview>

### Skeleton Loading

<ComponentPreview code={`<Tree skeleton={loading} skeleton_count={7} skeleton_depth={3} data={loading ? [] : data} />`}>
	<TreeSkeletonDemo client:load />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `data` | `TreeNode[] \| FlatTreeNode[]` | required | Tree data (nested or flat with parent IDs) |
| `selected` | `string[]` | `[]` | Selected node IDs (bindable) |
| `expanded` | `string[]` | `[]` | Expanded node IDs (bindable) |
| `selectable` | `boolean` | `false` | Enable node selection |
| `multi_select` | `boolean` | `false` | Allow multiple selection |
| `checkboxes` | `boolean` | `false` | Show checkboxes for selection |
| `show_lines` | `boolean` | `false` | Show connecting lines between nodes |
| `draggable` | `boolean` | `false` | Enable drag-and-drop reordering |
| `filter` | `string` | - | Search/filter term to highlight and filter matches |
| `dense` | `boolean` | `false` | Compact node spacing |
| `comfortable` | `boolean` | `false` | Relaxed node spacing |
| `skeleton` | `boolean` | `false` | Show loading skeleton |
| `skeleton_count` | `number` | `5` | Number of skeleton nodes |
| `skeleton_depth` | `number` | `2` | Nesting depth of skeleton nodes |
| `id` | `string` | auto | Element ID |
| `class` | `string` | `''` | Additional CSS classes |
| `load_children` | `(node: TreeNode) => Promise<TreeNode[]>` | - | Lazy load children on expand |
| `node_content` | `Snippet<[{ node: TreeNode; level: number }]>` | - | Custom node content renderer |

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `onselect` | `{ node: TreeNode, selected: string[] }` | Selection changed |
| `onexpand` | `{ node: TreeNode, expanded: boolean }` | Node expanded or collapsed |
| `ondrop` | `{ node: TreeNode, target: TreeNode, position: 'before' \| 'after' \| 'inside' }` | Node dropped after drag |

## Types

### TreeNode

The nested tree data format:

```typescript
interface TreeNode {
  id: string;
  label: string;
  icon?: Component;
  children?: TreeNode[];
  disabled?: boolean;
  data?: unknown;
}
```

### FlatTreeNode

The flat data format with parent references:

```typescript
interface FlatTreeNode {
  id: string;
  parentId: string | null;
  label: string;
  icon?: Component;
  disabled?: boolean;
  data?: unknown;
}
```

## Keyboard Navigation

| Key | Action |
|-----|--------|
| Arrow Down | Move focus to next visible node |
| Arrow Up | Move focus to previous visible node |
| Arrow Right | Expand focused node, or move to first child if already expanded |
| Arrow Left | Collapse focused node, or move to parent if already collapsed |
| Enter / Space | Select or toggle checkbox on focused node |
| Home | Move focus to first visible node |
| End | Move focus to last visible node |
| * (asterisk) | Expand all siblings of the focused node |

## Accessibility

- Container has `role="tree"`, each node has `role="treeitem"`
- `aria-expanded` on expandable nodes
- `aria-selected` on selectable nodes
- `aria-level` indicating nesting depth
- `aria-activedescendant` for focus management
- `aria-multiselectable` when multiple selection is enabled
- `aria-disabled` on disabled nodes
- Full keyboard navigation with arrow keys, Home, End, Enter, Space
- Expand/collapse animations respect `prefers-reduced-motion`
- Search matches are highlighted with `<mark>` elements
