---
title: Progress
description: A unified progress and loading indicator component supporting circular spinners, linear bars, overlays, and fullscreen loading states.
---

import { Progress } from '@delightstack/components/feedback';
import ComponentPreview from '../../../../components/ComponentPreview.astro';
import ProgressMultiSegmentDemo from '../../../../components/demos/ProgressMultiSegmentDemo.svelte';
import ProgressOverlayDemo from '../../../../components/demos/ProgressOverlayDemo.svelte';

## Import

```svelte
import { Progress } from '@delightstack/components';
```

## Basic Usage

By default, Progress renders as a circular indeterminate spinner.

<ComponentPreview code={`<Progress />`}>
	<Progress client:load />
</ComponentPreview>

## Examples

### Indeterminate Spinner

Omit the `value` prop for an indeterminate loading animation.

<ComponentPreview code={`<!-- Default circular spinner -->
<Progress />

<!-- Linear indeterminate bar -->
<Progress circular={false} />`}>
	<Progress client:load />
	<Progress client:load circular={false} />
</ComponentPreview>

### Determinate Progress

Provide a `value` (0-100) to show progress.

<ComponentPreview code={`<!-- Circular progress with percentage -->
<Progress value={75} showValue />

<!-- Linear progress bar -->
<Progress circular={false} value={45} />

<!-- Linear with label and percentage -->
<Progress circular={false} value={45} showValue label="Uploading..." />`}>
	<Progress client:load value={75} showValue />
	<Progress client:load circular={false} value={45} />
	<Progress client:load circular={false} value={45} showValue label="Uploading..." />
</ComponentPreview>

### Sizes

<ComponentPreview code={`<!-- Circular sizes -->
<Progress size="00" />
<Progress size="0" />
<Progress size="1" />
<Progress size="2" />
<Progress size="3" />`} title="Circular sizes">
	<Progress client:load size="00" />
	<Progress client:load size="0" />
	<Progress client:load size="1" />
	<Progress client:load size="2" />
	<Progress client:load size="3" />
</ComponentPreview>

<ComponentPreview code={`<!-- Linear sizes -->
<Progress circular={false} value={60} size="00" />
<Progress circular={false} value={60} size="1" />
<Progress circular={false} value={60} size="3" />`} title="Linear sizes">
	<Progress client:load circular={false} value={60} size="00" />
	<Progress client:load circular={false} value={60} size="1" />
	<Progress client:load circular={false} value={60} size="3" />
</ComponentPreview>

### Striped Bar

Add an animated stripe effect to a linear progress bar.

<ComponentPreview code={`<Progress circular={false} value={60} striped />`}>
	<Progress client:load circular={false} value={60} striped />
</ComponentPreview>

### Multi-Segment Progress

Display multiple segments with different colors in a single linear bar.

<ComponentPreview code={`<Progress
  circular={false}
  segments={[
    { value: 40, color: 'var(--color-success)', label: 'Documents' },
    { value: 25, color: 'var(--color-warning)', label: 'Photos' },
    { value: 10, color: 'var(--color-error)', label: 'System' },
  ]}
/>`}>
  <ProgressMultiSegmentDemo client:load />
</ComponentPreview>

### Overlay and Fullscreen

Use `overlay` to cover a parent element, or `fullScreen` to cover the entire viewport.

<ComponentPreview code={`<script>
  import { Progress } from '@delightstack/components';

  let isLoading = $state(false);
</script>

<button onclick={() => { isLoading = true; setTimeout(() => isLoading = false, 3000); }}>
  Reload Table Data
</button>

<div style="position: relative;">
  <table>...</table>
  {#if isLoading}
    <Progress overlay label="Loading data..." />
  {/if}
</div>`}>
  <ProgressOverlayDemo client:load />
</ComponentPreview>

### Color Variants

<ComponentPreview code={`<Progress value={100} success />
<Progress value={30} error />`}>
	<Progress client:load value={100} success />
	<Progress client:load value={30} error />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `number` | - | Progress value (0-100). Omit for indeterminate |
| `max` | `number` | `100` | Maximum value |
| `circular` | `boolean` | `true` | Circular mode (false = linear bar) |
| `loading` | `boolean` | `false` | Force indeterminate animation regardless of value |
| `size` | `'00' \| '0' \| '1' \| '2' \| '3'` | `'1'` | Size of the indicator |
| `color` | `string` | - | Custom fill color (overrides `currentColor`) |
| `label` | `string` | - | Text label (below spinner or beside bar) |
| `showValue` | `boolean` | `false` | Display the current percentage |
| `striped` | `boolean` | `false` | Striped animation on fill (linear mode only) |
| `overlay` | `boolean` | `false` | Cover parent element with backdrop |
| `fullScreen` | `boolean` | `false` | Cover entire viewport |
| `success` | `boolean` | `false` | Success color variant |
| `error` | `boolean` | `false` | Error color variant |
| `segments` | `ProgressSegment[]` | - | Multi-segment progress (linear only) |
| `id` | `string` | auto | Element ID |
| `class` | `string` | `''` | Additional CSS classes |

## Types

```typescript
interface ProgressSegment {
  value: number;
  color?: string;
  label?: string;
}
```

## Accessibility

- Uses `role="progressbar"` for determinate mode with `aria-valuenow`, `aria-valuemin`, and `aria-valuemax`
- Uses `role="status"` for indeterminate mode
- `aria-label` provides context (e.g., "Uploading file" or "75% complete")
- Respects `prefers-reduced-motion` -- shows a static partial arc instead of spinning
- Overlay mode uses `backdrop-filter: blur(2px)` for subtle background blur
