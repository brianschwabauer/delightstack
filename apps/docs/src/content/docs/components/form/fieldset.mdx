---
title: Fieldset
description: A semantic grouping component for related form fields with support for bordered, card, and filled visual styles, grid layout, and collapsible sections.
---

import { Fieldset, Input } from '@delightstack/components/form';
import ComponentPreview from '../../../../components/ComponentPreview.astro';
import FieldsetGridDemo from '../../../../components/demos/FieldsetGridDemo.svelte';
import FieldsetCollapsibleDemo from '../../../../components/demos/FieldsetCollapsibleDemo.svelte';
import FieldsetDescErrorDemo from '../../../../components/demos/FieldsetDescErrorDemo.svelte';
import FieldsetSkeletonDemo from '../../../../components/demos/FieldsetSkeletonDemo.svelte';

## Import

```svelte
import { Fieldset } from '@delightstack/components';
```

## Basic Usage

<ComponentPreview code={`<Fieldset legend="Personal Information">
${'  '}<Input label="First Name" name="firstName" />
${'  '}<Input label="Last Name" name="lastName" />
${'  '}<Input label="Email" type="email" name="email" />
</Fieldset>`}>
	<Fieldset client:load legend="Personal Information">
		<Input client:load label="First Name" name="firstName" />
		<Input client:load label="Last Name" name="lastName" />
		<Input client:load label="Email" type="email" name="email" />
	</Fieldset>
</ComponentPreview>

## Examples

### Visual Styles

Choose between default (no border), bordered, card, and filled styles. Only one style boolean should be set at a time.

<ComponentPreview title="Bordered" code={`<Fieldset bordered legend="Account Settings">
${'  '}<Input label="Username" name="username" required />
${'  '}<Input label="Password" type="password" name="password" required />
</Fieldset>`}>
	<Fieldset client:load bordered legend="Account Settings">
		<Input client:load label="Username" name="username" required />
		<Input client:load label="Password" type="password" name="password" required />
	</Fieldset>
</ComponentPreview>

<ComponentPreview title="Card" code={`<Fieldset card legend="Payment Information">
${'  '}<Input label="Card Number" name="cardNumber" />
${'  '}<Input label="Expiry" name="expiry" />
</Fieldset>`}>
	<Fieldset client:load card legend="Payment Information">
		<Input client:load label="Card Number" name="cardNumber" />
		<Input client:load label="Expiry" name="expiry" />
	</Fieldset>
</ComponentPreview>

<ComponentPreview title="Filled" code={`<Fieldset filled legend="Optional Details">
${'  '}<Input label="Nickname" name="nickname" />
</Fieldset>`}>
	<Fieldset client:load filled legend="Optional Details">
		<Input client:load label="Nickname" name="nickname" />
	</Fieldset>
</ComponentPreview>

### Grid Layout

Use `grid` to arrange children in a CSS grid. Children can span columns with inline styles.

<ComponentPreview code={`<Fieldset legend="Address" grid columns={2}>
${'  '}<Input label="Street Address" name="street" style="grid-column: 1 / -1" />
${'  '}<Input label="City" name="city" />
${'  '}<Input label="State" name="state" />
${'  '}<Input label="ZIP Code" name="zip" />
${'  '}<Input label="Country" name="country" />
</Fieldset>`}>
	<FieldsetGridDemo client:load />
</ComponentPreview>

### Collapsible

Click the legend to expand or collapse the content. Bind `collapsed` for controlled state.

<ComponentPreview code={`<script>
${'  '}import { Fieldset, Input, Toggle } from '@delightstack/components';

${'  '}let collapsed = $state(false);
</script>

<Fieldset legend="Advanced Options" collapsible bind:collapsed filled>
${'  '}<Input label="Custom Domain" name="domain" />
${'  '}<Toggle label="Enable API Access" name="api" />
</Fieldset>`}>
	<FieldsetCollapsibleDemo client:load />
</ComponentPreview>

### With Description and Error

<ComponentPreview code={`<Fieldset legend="Profile" description="This information is publicly visible" bordered>
${'  '}<Input label="Display Name" name="displayName" />
${'  '}<Input label="Bio" name="bio" />
</Fieldset>

<Fieldset legend="Permissions" required error="Select at least one permission" bordered>
${'  '}<Checkbox checked={false} label="Read" />
${'  '}<Checkbox checked={false} label="Write" />
</Fieldset>`}>
	<FieldsetDescErrorDemo client:load />
</ComponentPreview>

### Skeleton Loading

<ComponentPreview code={`<Fieldset skeleton legend="User Profile">
${'  '}<Input label="Name" name="name" skeleton />
${'  '}<Input label="Email" name="email" skeleton />
</Fieldset>`}>
	<FieldsetSkeletonDemo client:load />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `legend` | `string` | - | Section title rendered as `<legend>` |
| `description` | `string` | - | Helper text below legend |
| `bordered` | `boolean` | `false` | Subtle border style |
| `card` | `boolean` | `false` | Card style with shadow |
| `filled` | `boolean` | `false` | Light background fill style |
| `disabled` | `boolean` | `false` | Disable all child fields natively |
| `error` | `string` | - | Group-level error message |
| `required` | `boolean` | `false` | Show required asterisk on legend |
| `collapsible` | `boolean` | `false` | Allow collapsing the content |
| `collapsed` | `boolean` | `false` | Collapsed state, bindable |
| `grid` | `boolean` | `false` | Enable CSS Grid layout for children |
| `columns` | `number` | `2` | Number of grid columns (when `grid` is true) |
| `skeleton` | `boolean` | `false` | Show skeleton loading state |
| `dense` | `boolean` | `false` | Compact spacing between fields |
| `comfortable` | `boolean` | `false` | Relaxed spacing between fields |
| `id` | `string` | auto | Element ID |
| `class` | `string` | `''` | Additional CSS classes |
| `children` | `Snippet` | - | Form field content |

## Accessibility

- Semantic `<fieldset>` and `<legend>` HTML elements
- `aria-describedby` links to description and error text
- `aria-required` when `required` is set
- Collapsible legend is a `<button>` with `aria-expanded`
- Native `<fieldset disabled>` propagates disabled state to all child form controls
- Error message uses `role="alert"` for screen reader announcements
- Required indicator shows red asterisk with `aria-hidden="true"`
