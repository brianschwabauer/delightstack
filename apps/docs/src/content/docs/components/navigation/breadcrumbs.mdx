---
title: Breadcrumbs
description: A navigation trail with automatic home icon, SSR-safe auto-collapsing, Schema.org structured data, and customizable separators.
---

import { Breadcrumbs } from '@delightstack/components/navigation';
import ComponentPreview from '../../../../components/ComponentPreview.astro';
import BreadcrumbsSeparatorDemo from '../../../../components/demos/BreadcrumbsSeparatorDemo.svelte';
import BreadcrumbsClickDemo from '../../../../components/demos/BreadcrumbsClickDemo.svelte';
import BreadcrumbsAutoCollapseDemo from '../../../../components/demos/BreadcrumbsAutoCollapseDemo.svelte';
import BreadcrumbsSkeletonDemo from '../../../../components/demos/BreadcrumbsSkeletonDemo.svelte';

## Import

```svelte
import { Breadcrumbs } from '@delightstack/components';
import type { BreadcrumbItem } from '@delightstack/components';
```

## Basic Usage

<ComponentPreview code={`<script>
${'  '}import { Breadcrumbs } from '@delightstack/components';
${'  '}import type { BreadcrumbItem } from '@delightstack/components';

${'  '}const items: BreadcrumbItem[] = [
${'  '}${'  '}{ label: 'Products', href: '/products' },
${'  '}${'  '}{ label: 'Electronics', href: '/products/electronics' },
${'  '}${'  '}{ label: 'Headphones' },
${'  '}];
</script>

<Breadcrumbs {items} />`}>
	<Breadcrumbs client:load home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Electronics', href: '#electronics' }, { label: 'Headphones' }]} />
</ComponentPreview>

## Examples

### Auto-Collapse to Fit

With no `max_items` set, the trail auto-collapses to fit its container — the first
item and the last two stay pinned while the middle items fold into the “…” menu,
starting from the center. This is done in **pure CSS** with container queries, so
it's already correct in server-rendered HTML (no layout flash, no JS measuring).

<ComponentPreview code={`<!-- Just render the items — collapsing is automatic -->
<Breadcrumbs {items} />`}>
	<BreadcrumbsAutoCollapseDemo client:load />
</ComponentPreview>

### Collapsed Middle Items (explicit)

Pass `max_items` to force the whole middle block into the dropdown at a fixed count.

<ComponentPreview code={`<script>
${'  '}import { Breadcrumbs } from '@delightstack/components';

${'  '}const items = [
${'  '}${'  '}{ label: 'Category', href: '/cat' },
${'  '}${'  '}{ label: 'Subcategory', href: '/cat/sub' },
${'  '}${'  '}{ label: 'Brand', href: '/cat/sub/brand' },
${'  '}${'  '}{ label: 'Model', href: '/cat/sub/brand/model' },
${'  '}${'  '}{ label: 'Product' },
${'  '}];
</script>

<Breadcrumbs {items} max_items={3} />`}>
	<Breadcrumbs client:load home_href="#" items={[{ label: 'Category', href: '#cat' }, { label: 'Subcategory', href: '#sub' }, { label: 'Brand', href: '#brand' }, { label: 'Model', href: '#model' }, { label: 'Product' }]} max_items={3} />
</ComponentPreview>

### Dense

Tightens the spacing between items and separators for compact layouts like toolbars.

<ComponentPreview code={`<Breadcrumbs {items} dense />`}>
	<Breadcrumbs client:load dense home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Electronics', href: '#electronics' }, { label: 'Headphones' }]} />
</ComponentPreview>

### Without Home Icon

Hide the automatic home breadcrumb.

<ComponentPreview code={`<Breadcrumbs {items} show_home={false} />`}>
	<Breadcrumbs client:load show_home={false} items={[{ label: 'Products', href: '#products' }, { label: 'Electronics', href: '#electronics' }, { label: 'Headphones' }]} />
</ComponentPreview>

### Custom Separator

Replace the default chevron separator with a custom snippet.

<ComponentPreview code={`<Breadcrumbs {items}>
${'  '}{#snippet separator()}
${'  '}${'  '}<span>/</span>
${'  '}{/snippet}
</Breadcrumbs>`}>
	<BreadcrumbsSeparatorDemo client:load />
</ComponentPreview>

### Sizes

<ComponentPreview code={`<Breadcrumbs {items} size="0" />  <!-- Small -->
<Breadcrumbs {items} size="1" />  <!-- Default -->
<Breadcrumbs {items} size="2" />  <!-- Medium -->
<Breadcrumbs {items} size="3" />  <!-- Large -->`}>
	<div style="display: flex; flex-direction: column; gap: 0.75rem;">
		<Breadcrumbs client:load home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Headphones' }]} size="0" />
		<Breadcrumbs client:load home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Headphones' }]} size="1" />
		<Breadcrumbs client:load home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Headphones' }]} size="2" />
		<Breadcrumbs client:load home_href="#" items={[{ label: 'Products', href: '#products' }, { label: 'Headphones' }]} size="3" />
	</div>
</ComponentPreview>

### Click Handler

React to breadcrumb clicks without relying solely on native navigation.

<ComponentPreview code={`<script>
${'  '}import { Breadcrumbs } from '@delightstack/components';

${'  '}const items = [
${'  '}${'  '}{ label: 'Home', href: '/' },
${'  '}${'  '}{ label: 'Products', href: '/products' },
${'  '}${'  '}{ label: 'Detail' },
${'  '}];

${'  '}let lastClicked = $state('');
</script>

<Breadcrumbs
${'  '}{items}
${'  '}show_home={false}
${'  '}onclick={({ item, index }) => {
${'  '}${'  '}lastClicked = \`Clicked "\${item.label}" (index \${index})\`;
${'  '}}}
/>`}>
	<BreadcrumbsClickDemo client:load />
</ComponentPreview>

### Skeleton Loading

The skeleton mirrors the real trail's box exactly (size and spacing), so swapping
between loading and loaded states never shifts the layout. It scales with `size`,
shows the home icon when `show_home` is set, and is automatically replaced the
moment real `items` are provided.

<ComponentPreview code={`<!-- skeleton shows only while items is empty -->
<Breadcrumbs skeleton skeleton_count={3} items={loading ? [] : items} />`}>
	<BreadcrumbsSkeletonDemo client:load />
</ComponentPreview>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `items` | `BreadcrumbItem[]` | `[]` | The breadcrumb items to display |
| `max_items` | `number` | `undefined` | Max visible items before collapsing the middle block into the ellipsis dropdown. When omitted, the trail auto-collapses to fit its container width (pure CSS, SSR-safe) |
| `show_home` | `boolean` | `true` | Whether to show a home icon as the first breadcrumb |
| `home_href` | `string` | `'/'` | The href for the home breadcrumb |
| `size` | `'0' \| '1' \| '2' \| '3'` | `'1'` | Size of the breadcrumbs |
| `dense` | `boolean` | `false` | Condensed spacing between items and separators |
| `skeleton` | `boolean` | `false` | Show skeleton loading state (only while `items` is empty) |
| `skeleton_count` | `number` | `3` | Number of skeleton placeholder items |
| `id` | `string` | auto | Element ID |
| `class` | `string` | `''` | Additional CSS classes |
| `children` | `Snippet` | `undefined` | Custom rendering snippet (replaces default rendering) |
| `separator` | `Snippet` | `undefined` | Custom separator snippet |

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `onclick` | `{ item: BreadcrumbItem, index: number }` | Fires when a breadcrumb item is clicked |

## Types

### BreadcrumbItem

```typescript
interface BreadcrumbItem {
  label: string;
  href?: string;
}
```

## Auto-collapse

When `max_items` is omitted, the component estimates each item's width from its
label length and uses CSS [container query units](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_containment/Container_queries)
to decide what fits. Because the decision is made entirely in CSS, the correct
collapsed state is present in the first server-rendered paint — there's no
measure-then-reflow flash. The first item and the last two are always kept; the
middle items fold into the “…” menu from the center outward as space shrinks.

Estimates are intentionally approximate (proportional fonts vary), so leave a
little breathing room if precise control matters — or set `max_items` explicitly.

## Accessibility

- Uses `<nav>` with `aria-label="Breadcrumb"`
- Semantic `<ol>` list for the breadcrumb trail
- Last item has `aria-current="page"`
- Home icon includes screen-reader-only text (`<span class="sr-only">`)
- The ellipsis uses a native `<details>`/`<summary>` disclosure (keyboard- and
  screen-reader-friendly, works without JS)
- Collapsed (zero-width) copies are marked `inert` when JS is available, so only
  the visible copy is focusable and announced
- Automatically generates Schema.org `BreadcrumbList` JSON-LD for SEO
